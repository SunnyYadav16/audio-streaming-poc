# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.128.4
uvicorn==0.40.0
websockets==16.0
python-multipart==0.0.22
python-dotenv==1.2.1

# ── Audio I/O ────────────────────────────────────────────────────────────────
av==16.1.0

# ── ML / AI ──────────────────────────────────────────────────────────────────
torch==2.10.0
numpy==2.4.2

# ASR — Faster Whisper (depends on ctranslate2 internally)
faster-whisper==1.2.1
ctranslate2==4.7.1

# MT — NLLB-200 via Hugging Face Transformers
transformers==5.1.0
sentencepiece==0.2.1

# TTS — Piper
piper-tts==1.4.1

# ── Google Auth (for Vertex AI / LLaMA legal verifier) ───────────────────────
google-auth==2.48.0

# ── OpenAI SDK (Vertex AI OpenAI-compatible endpoint) ────────────────────────
openai
